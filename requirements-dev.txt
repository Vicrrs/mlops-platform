# Dependências para rodar os testes dos scripts de governança (scripts/*.py)
# e o lint do repositório central. Os scripts em si só importam mlflow e a
# biblioteca padrão -- nenhum deles depende de PySpark.
mlflow==2.17.2
pytest>=8.0
pytest-cov>=5.0
pyyaml>=6.0
ruff>=0.6
